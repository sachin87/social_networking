= simple_form_for @page, :html => { :class => 'form-horizontal' } do |f|
  = f.input :title
  = f.input :permalink
  = f.input :body
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), page_path, :class => 'btn'
